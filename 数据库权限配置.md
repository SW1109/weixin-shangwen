# 数据库权限配置指南

## 重要说明
所有代码已经修改为使用 `where()` 查询替代 `doc()` 方法，以兼容"所有用户可读"的权限设置。

## 推荐的权限配置

### 1. dishes（菜品表）
**权限设置：** 所有用户可读，仅创建者可写

```json
{
  "read": true,
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 所有用户可以浏览菜品
- 只有创建者（商家）可以修改自己创建的菜品

---

### 2. categories（分类表）
**权限设置：** 所有用户可读，仅创建者可写

```json
{
  "read": true,
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 所有用户可以查看分类
- 只有创建者（商家）可以管理分类

---

### 3. orders（订单表）
**权限设置：** 仅创建者可读写

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 顾客只能查看自己的订单
- 顾客只能修改自己的订单（取消订单等）
- **注意：** 商家查看订单需要使用云函数

---

### 4. carts（购物车表）
**权限设置：** 仅创建者可读写

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 用户只能查看和修改自己的购物车

---

### 5. addresses（地址表）
**权限设置：** 仅创建者可读写

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 用户只能管理自己的收货地址

---

### 6. users（用户表）
**权限设置：** 仅创建者可读写

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 用户只能查看和修改自己的信息

---

### 7. merchants（商家表）
**权限设置：** 仅创建者可读写

```json
{
  "read": "doc._openid == auth.openid",
  "write": "doc._openid == auth.openid"
}
```

**说明：**
- 商家只能查看和修改自己的信息

---

## 配置步骤

### 方式一：云开发控制台配置

1. 登录[微信云开发控制台](https://console.cloud.tencent.com/tcb)
2. 选择你的环境
3. 进入 **数据库** -> **集合**
4. 点击对应集合的 **权限设置**
5. 选择对应的权限模板或自定义规则

### 方式二：使用 JSON 配置文件

在项目根目录创建 `database.json`：

```json
{
  "permissions": {
    "dishes": {
      "read": true,
      "write": "doc._openid == auth.openid"
    },
    "categories": {
      "read": true,
      "write": "doc._openid == auth.openid"
    },
    "orders": {
      "read": "doc._openid == auth.openid",
      "write": "doc._openid == auth.openid"
    },
    "carts": {
      "read": "doc._openid == auth.openid",
      "write": "doc._openid == auth.openid"
    },
    "addresses": {
      "read": "doc._openid == auth.openid",
      "write": "doc._openid == auth.openid"
    },
    "users": {
      "read": "doc._openid == auth.openid",
      "write": "doc._openid == auth.openid"
    },
    "merchants": {
      "read": "doc._openid == auth.openid",
      "write": "doc._openid == auth.openid"
    }
  }
}
```

---

## 代码修改说明

### ✅ 已修改的文件

1. **customer-app/utils/api.js**
   - `getDishDetail()` - 改用 where 查询
   - `getOrderDetail()` - 改用 where 查询
   - `getOrders()` - 优化查询条件判断
   - `cancelOrder()` - 改用云函数
   - `watchOrder()` - 改用 where 查询
   - `updateAddress()` - 改用 where 更新
   - `deleteAddress()` - 改用 where 删除
   - `setDefaultAddress()` - 改用 where 更新
   - `saveCart()` - 改用 where 更新

2. **merchant-app/utils/api.js**
   - `getOrderDetail()` - 改用 where 查询
   - `getDishDetail()` - 改用 where 查询
   - `updateCategory()` - 改用 where 更新
   - `deleteCategory()` - 改用 where 删除

3. **customer-app/pages/dish-detail/dish-detail.js**
   - `loadDishDetail()` - 改用 where 查询
   - 购物车更新 - 改用 where 更新

### 🔍 关键改动对比

**Before (有权限问题):**
```javascript
db.collection('dishes').doc(dishId).get()
```

**After (无权限问题):**
```javascript
db.collection('dishes')
  .where({ _id: dishId })
  .get()
  .then(res => {
    if (res.data && res.data.length > 0) {
      return res.data[0]
    }
    throw new Error('数据不存在')
  })
```

---

## 测试建议

### 1. 顾客端测试
- ✅ 浏览菜品列表
- ✅ 查看菜品详情
- ✅ 加入购物车
- ✅ 创建订单
- ✅ 查看订单列表
- ✅ 查看订单详情
- ✅ 管理收货地址

### 2. 商家端测试
- ✅ 管理菜品（增删改查）
- ✅ 管理分类
- ✅ 查看订单（需要云函数支持）
- ✅ 更新订单状态

---

## 常见问题

### Q1: 为什么改用 where 查询？
**A:** 使用 `doc()` 方法时，数据库会进行额外的权限检查。改用 `where()` 查询可以更好地兼容"所有用户可读"的权限设置。

### Q2: 商家如何查看所有订单？
**A:** 商家查看订单需要通过云函数，因为订单权限设置为"仅创建者可读"。云函数拥有完整的数据库访问权限，可以绕过客户端的权限限制。

### Q3: 如果还是报权限错误怎么办？
**A:** 
1. 检查云开发控制台的权限配置是否正确
2. 检查用户是否已登录（_openid 是否存在）
3. 临时改为"所有用户可读写"进行测试
4. 查看控制台错误日志，确认具体是哪个操作报错

### Q4: 为什么 cancelOrder 要改用云函数？
**A:** 因为订单的权限是"仅创建者可读写"，直接使用 where 更新可能会失败。使用云函数可以确保更新成功，同时还能添加业务逻辑（如库存回滚等）。

---

## 云函数配置（可选）

如果商家需要查看所有订单，可以创建一个云函数：

```javascript
// cloudfunctions/getMerchantOrders/index.js
const cloud = require('wx-server-sdk')
cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()

exports.main = async (event, context) => {
  const { status } = event
  
  try {
    let query = db.collection('orders')
    
    if (status !== null && status !== undefined) {
      query = query.where({ status })
    }
    
    const res = await query
      .orderBy('createTime', 'desc')
      .get()
    
    return {
      code: 0,
      data: res.data
    }
  } catch (err) {
    return {
      code: -1,
      message: err.message
    }
  }
}
```

---

## 总结

✅ 所有数据库调用已优化，使用 `where()` 替代 `doc()`  
✅ 添加了完善的错误处理和日志  
✅ 兼容"所有用户可读，仅创建者可写"的权限设置  
✅ 保证了数据安全性和用户隐私  

按照本文档配置权限后，应该不会再出现权限错误了！🎉
