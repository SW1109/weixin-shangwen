# 点餐微信小程序系统 - 系统设计

## 一、系统架构设计

### 1.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                     微信小程序平台                        │
├──────────────────────┬──────────────────────────────────┤
│   客户小程序          │      商家管理小程序                │
│   (customer-app)     │      (merchant-app)              │
├──────────────────────┴──────────────────────────────────┤
│                 腾讯云开发 (CloudBase)                    │
│  ┌────────────┬────────────┬────────────┬────────────┐ │
│  │ 云数据库    │  云函数     │  云存储     │   云调用    │ │
│  │ (Database) │ (Function) │ (Storage)  │  (API)     │ │
│  └────────────┴────────────┴────────────┴────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 1.2 架构说明

#### 1.2.1 客户小程序 (customer-app)
- **功能**：提供客户点餐、订单管理、个人中心等功能
- **技术**：微信小程序原生开发（WXML、WXSS、JS）
- **数据交互**：通过 `wx.cloud.database()` 直接操作数据库
- **文件上传**：使用云存储上传用户头像等

#### 1.2.2 商家管理小程序 (merchant-app)
- **功能**：提供菜品管理、订单处理、数据统计等功能
- **技术**：微信小程序原生开发（WXML、WXSS、JS）
- **数据交互**：通过云数据库和云函数操作数据
- **文件上传**：使用云存储上传菜品图片

#### 1.2.3 云开发服务
- **云数据库**：存储用户、菜品、订单等数据
- **云函数**：处理复杂业务逻辑（如登录、支付、统计）
- **云存储**：存储图片资源
- **云调用**：调用微信开放接口

## 二、数据库设计

### 2.1 数据库集合（Collection）

#### 2.1.1 用户集合 (users)
```javascript
{
  _id: "auto_generated",        // 自动生成的 ID
  _openid: "user_openid",       // 微信 openid（系统自动添加）
  nickName: "张三",              // 用户昵称
  avatarUrl: "https://...",     // 用户头像
  phoneNumber: "13800138000",   // 手机号
  createTime: Date,             // 注册时间
  updateTime: Date              // 更新时间
}
```

**索引**：
- `_openid`（唯一索引）

**权限规则**：
```json
{
  "read": "auth.openid == doc._openid",
  "write": "auth.openid == doc._openid"
}
```

#### 2.1.2 菜品分类集合 (categories)
```javascript
{
  _id: "auto_generated",
  name: "热销推荐",              // 分类名称
  sort: 1,                      // 排序（数字越小越靠前）
  icon: "https://...",          // 分类图标（可选）
  status: 1,                    // 状态：1-启用，0-禁用
  createTime: Date,
  updateTime: Date
}
```

**索引**：
- `sort`（升序）

**权限规则**：
```json
{
  "read": true,
  "write": false
}
```
（仅管理员可写，通过云函数实现）

#### 2.1.3 菜品集合 (dishes)
```javascript
{
  _id: "auto_generated",
  name: "宫保鸡丁",              // 菜品名称
  categoryId: "category_id",    // 分类 ID
  categoryName: "热销推荐",      // 分类名称（冗余，提高查询性能）
  image: "cloud://...",         // 菜品图片（云存储路径）
  price: 28.00,                 // 价格（单位：元）
  originalPrice: 38.00,         // 原价（可选，用于显示优惠）
  description: "经典川菜...",    // 菜品描述
  stock: 100,                   // 库存数量（-1 表示无限库存）
  sales: 256,                   // 销量
  status: 1,                    // 状态：1-上架，0-下架
  isRecommend: false,           // 是否推荐
  tags: ["辣", "热销"],          // 标签
  createTime: Date,
  updateTime: Date
}
```

**索引**：
- `categoryId`
- `status`
- `sales`（降序）

**权限规则**：
```json
{
  "read": "doc.status == 1",
  "write": false
}
```
（仅管理员可写）

#### 2.1.4 订单集合 (orders)
```javascript
{
  _id: "auto_generated",
  orderNo: "202401070001",      // 订单编号
  _openid: "user_openid",       // 用户 openid
  userInfo: {                   // 用户信息
    nickName: "张三",
    avatarUrl: "https://...",
    phoneNumber: "13800138000"
  },
  dishes: [                     // 订单菜品
    {
      dishId: "dish_id",
      name: "宫保鸡丁",
      image: "cloud://...",
      price: 28.00,
      quantity: 2
    }
  ],
  totalAmount: 56.00,           // 订单总金额
  address: {                    // 配送地址
    name: "张三",
    phone: "13800138000",
    province: "广东省",
    city: "深圳市",
    district: "南山区",
    detail: "科技园XX号"
  },
  remark: "少放辣",              // 备注
  status: 1,                    // 订单状态：1-待支付，2-待配送，3-配送中，4-已完成，5-已取消
  payStatus: 0,                 // 支付状态：0-未支付，1-已支付
  payTime: Date,                // 支付时间
  createTime: Date,             // 下单时间
  updateTime: Date,             // 更新时间
  completeTime: Date            // 完成时间
}
```

**索引**：
- `_openid`
- `orderNo`（唯一索引）
- `status`
- `createTime`（降序）

**权限规则**：
```json
{
  "read": "auth.openid == doc._openid || get('database.merchants.${auth.openid}').role == 'admin'",
  "write": "auth.openid == doc._openid && doc.status == 1"
}
```

#### 2.1.5 收货地址集合 (addresses)
```javascript
{
  _id: "auto_generated",
  _openid: "user_openid",
  name: "张三",                  // 收货人
  phone: "13800138000",         // 联系电话
  province: "广东省",
  city: "深圳市",
  district: "南山区",
  detail: "科技园XX号",          // 详细地址
  isDefault: true,              // 是否默认地址
  createTime: Date,
  updateTime: Date
}
```

**索引**：
- `_openid`
- `isDefault`

**权限规则**：
```json
{
  "read": "auth.openid == doc._openid",
  "write": "auth.openid == doc._openid"
}
```

#### 2.1.6 商家账号集合 (merchants)
```javascript
{
  _id: "auto_generated",
  _openid: "merchant_openid",   // 商家 openid
  username: "admin",            // 登录账号
  password: "hashed_password",  // 加密后的密码
  role: "admin",                // 角色：admin-管理员
  storeName: "美食餐厅",         // 店铺名称
  storePhone: "0755-12345678",  // 店铺电话
  storeAddress: "深圳市...",     // 店铺地址
  status: 1,                    // 状态：1-正常，0-禁用
  createTime: Date,
  updateTime: Date
}
```

**索引**：
- `_openid`（唯一索引）
- `username`（唯一索引）

**权限规则**：
```json
{
  "read": "auth.openid == doc._openid",
  "write": false
}
```
（仅通过云函数操作）

#### 2.1.7 购物车集合 (carts)
```javascript
{
  _id: "auto_generated",
  _openid: "user_openid",
  dishes: [                     // 购物车菜品
    {
      dishId: "dish_id",
      name: "宫保鸡丁",
      image: "cloud://...",
      price: 28.00,
      quantity: 2
    }
  ],
  updateTime: Date
}
```

**索引**：
- `_openid`（唯一索引）

**权限规则**：
```json
{
  "read": "auth.openid == doc._openid",
  "write": "auth.openid == doc._openid"
}
```

### 2.2 数据库设计说明

#### 2.2.1 数据冗余策略
- 在订单中冗余菜品信息（名称、价格、图片），防止菜品修改后历史订单数据变化
- 在菜品中冗余分类名称，减少联表查询

#### 2.2.2 数据一致性保证
- 使用云函数处理复杂的数据更新操作
- 使用数据库事务保证数据一致性
- 关键操作添加乐观锁机制

#### 2.2.3 数据安全
- 敏感数据（如密码）使用哈希加密
- 使用 CloudBase 安全规则限制数据访问
- 商家操作需要身份验证

## 三、云函数设计

### 3.1 云函数列表

#### 3.1.1 login (用户登录)
**功能**：获取用户 openid 并创建/更新用户信息

**输入参数**：
```javascript
{
  nickName: "张三",
  avatarUrl: "https://..."
}
```

**返回数据**：
```javascript
{
  code: 0,
  message: "登录成功",
  data: {
    openid: "user_openid",
    userInfo: { ... }
  }
}
```

#### 3.1.2 merchantLogin (商家登录)
**功能**：验证商家账号密码

**输入参数**：
```javascript
{
  username: "admin",
  password: "123456"
}
```

**返回数据**：
```javascript
{
  code: 0,
  message: "登录成功",
  data: {
    token: "jwt_token",
    merchantInfo: { ... }
  }
}
```

#### 3.1.3 createOrder (创建订单)
**功能**：创建订单并减少库存

**输入参数**：
```javascript
{
  dishes: [ ... ],
  totalAmount: 56.00,
  address: { ... },
  remark: "少放辣"
}
```

**返回数据**：
```javascript
{
  code: 0,
  message: "下单成功",
  data: {
    orderId: "order_id",
    orderNo: "202401070001"
  }
}
```

#### 3.1.4 updateOrderStatus (更新订单状态)
**功能**：商家更新订单状态

**输入参数**：
```javascript
{
  orderId: "order_id",
  status: 2
}
```

**返回数据**：
```javascript
{
  code: 0,
  message: "状态更新成功"
}
```

#### 3.1.5 payOrder (订单支付)
**功能**：模拟订单支付

**输入参数**：
```javascript
{
  orderId: "order_id"
}
```

**返回数据**：
```javascript
{
  code: 0,
  message: "支付成功"
}
```

#### 3.1.6 getStatistics (获取统计数据)
**功能**：获取销售统计数据

**输入参数**：
```javascript
{
  startDate: "2024-01-01",
  endDate: "2024-01-07"
}
```

**返回数据**：
```javascript
{
  code: 0,
  data: {
    totalSales: 12580.00,
    totalOrders: 156,
    topDishes: [ ... ]
  }
}
```

#### 3.1.7 manageDish (管理菜品)
**功能**：添加/编辑/删除菜品

**输入参数**：
```javascript
{
  action: "add" | "update" | "delete",
  dishData: { ... }
}
```

**返回数据**：
```javascript
{
  code: 0,
  message: "操作成功"
}
```

## 四、云存储设计

### 4.1 存储目录结构

```
cloud://env-id.xxxx/
├── dishes/                 # 菜品图片
│   ├── {dishId}.jpg
│   └── ...
├── categories/             # 分类图标
│   ├── {categoryId}.png
│   └── ...
└── avatars/               # 用户头像
    ├── {openid}.jpg
    └── ...
```

### 4.2 文件上传流程

1. 客户端选择图片
2. 使用 `wx.cloud.uploadFile()` 上传到云存储
3. 获取云文件 ID（cloud://...）
4. 将文件 ID 存储到数据库

### 4.3 文件访问控制

- 图片设置为公开读
- 仅授权用户可上传
- 文件大小限制：单个文件 < 2MB

## 五、页面结构设计

### 5.1 客户小程序页面结构

```
customer-app/
├── pages/
│   ├── index/                  # 首页（菜品列表）
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── dish-detail/            # 菜品详情
│   ├── cart/                   # 购物车
│   ├── order-confirm/          # 确认订单
│   ├── order-list/             # 订单列表
│   ├── order-detail/           # 订单详情
│   ├── address-list/           # 地址列表
│   ├── address-edit/           # 编辑地址
│   ├── user/                   # 个人中心
│   └── login/                  # 登录页
├── components/                 # 自定义组件
│   ├── dish-item/              # 菜品项组件
│   ├── cart-button/            # 购物车按钮
│   └── order-item/             # 订单项组件
├── utils/                      # 工具函数
│   ├── util.js                 # 通用工具
│   └── api.js                  # API 封装
├── app.js
├── app.json
└── app.wxss
```

### 5.2 商家管理小程序页面结构

```
merchant-app/
├── pages/
│   ├── login/                  # 登录页
│   ├── home/                   # 首页（统计概览）
│   ├── order-list/             # 订单列表
│   ├── order-detail/           # 订单详情
│   ├── dish-list/              # 菜品列表
│   ├── dish-edit/              # 编辑菜品
│   ├── category-manage/        # 分类管理
│   └── statistics/             # 数据统计
├── components/
│   ├── order-card/             # 订单卡片
│   ├── dish-card/              # 菜品卡片
│   └── chart/                  # 图表组件
├── utils/
│   ├── util.js
│   └── api.js
├── app.js
├── app.json
└── app.wxss
```

## 六、数据实时同步方案

### 6.1 客户端监听订单状态

```javascript
// 在订单详情页监听订单状态变化
const watcher = db.collection('orders')
  .doc(orderId)
  .watch({
    onChange: (snapshot) => {
      // 订单状态更新时刷新页面
      this.setData({
        order: snapshot.docs[0]
      })
    },
    onError: (err) => {
      console.error('监听失败', err)
    }
  })
```

### 6.2 商家端监听新订单

```javascript
// 在订单列表页监听新订单
const watcher = db.collection('orders')
  .where({
    status: 1  // 待处理订单
  })
  .watch({
    onChange: (snapshot) => {
      // 有新订单时提醒商家
      if (snapshot.docChanges.length > 0) {
        wx.showToast({
          title: '收到新订单',
          icon: 'success'
        })
        // 播放提示音
        this.playNotificationSound()
      }
      this.loadOrders()
    }
  })
```

### 6.3 菜品信息同步

- 商家修改菜品后，数据库立即更新
- 客户端下次进入页面时自动获取最新数据
- 对于已加入购物车的商品，提示用户价格变化

## 七、安全设计

### 7.1 数据库安全规则

```json
{
  "read": "根据集合设置不同的读权限",
  "write": "仅允许用户操作自己的数据或通过云函数操作"
}
```

### 7.2 云函数权限验证

```javascript
// 在云函数中验证商家身份
const db = cloud.database()
const merchants = await db.collection('merchants')
  .where({
    _openid: cloud.getWXContext().OPENID,
    status: 1
  })
  .get()

if (merchants.data.length === 0) {
  return {
    code: 403,
    message: '无权限操作'
  }
}
```

### 7.3 敏感操作保护

- 订单金额在服务端计算，防止客户端篡改
- 库存扣减使用事务，防止超卖
- 商家密码使用 bcrypt 加密

## 八、性能优化方案

### 8.1 数据库优化

- 合理设置索引，提高查询效率
- 使用分页加载，避免一次性加载大量数据
- 冗余常用字段，减少联表查询

### 8.2 图片优化

- 图片上传前压缩
- 使用云存储 CDN 加速
- 图片懒加载

### 8.3 缓存策略

- 菜品分类和列表使用本地缓存
- 设置合理的缓存过期时间
- 下拉刷新时更新缓存

### 8.4 代码优化

- 减少不必要的 setData 调用
- 使用防抖和节流优化频繁操作
- 组件化开发，提高代码复用性

## 九、测试方案

### 9.1 功能测试

- 用户登录流程
- 菜品浏览和搜索
- 购物车操作
- 订单创建和支付
- 商家管理功能

### 9.2 性能测试

- 页面加载时间
- 数据库查询效率
- 并发订单处理

### 9.3 安全测试

- 权限验证
- 数据访问控制
- SQL 注入防护

## 十、部署方案

### 10.1 云开发环境

1. 创建云开发环境
2. 配置数据库和云函数
3. 上传云函数代码
4. 设置安全规则

### 10.2 小程序发布

1. 客户小程序发布
2. 商家管理小程序发布
3. 提交审核
4. 正式上线

### 10.3 初始化数据

```javascript
// 初始化商家账号
db.collection('merchants').add({
  data: {
    username: 'admin',
    password: 'hashed_password',
    role: 'admin',
    storeName: '美食餐厅',
    status: 1,
    createTime: new Date()
  }
})

// 初始化菜品分类
db.collection('categories').add({
  data: [
    { name: '热销推荐', sort: 1, status: 1 },
    { name: '主食类', sort: 2, status: 1 },
    { name: '小吃类', sort: 3, status: 1 }
  ]
})
```

## 十一、运维监控

### 11.1 日志监控

- 云函数调用日志
- 数据库操作日志
- 错误日志收集

### 11.2 性能监控

- 接口响应时间
- 数据库查询性能
- 云存储流量

### 11.3 业务监控

- 订单量统计
- 用户活跃度
- 菜品销售排行

## 十二、技术选型总结

| 技术点 | 选型 | 说明 |
|--------|------|------|
| 前端框架 | 微信小程序原生 | 官方支持，性能最优 |
| 后端服务 | 腾讯云开发 CloudBase | 无需服务器，开发效率高 |
| 数据库 | CloudBase 云数据库 | NoSQL，灵活易用 |
| 文件存储 | CloudBase 云存储 | 集成方便，CDN 加速 |
| 身份认证 | 微信授权登录 | 用户体验好，安全性高 |
| 支付方式 | 微信支付（模拟） | 后续可接入真实支付 |

## 十三、开发计划

### 第一阶段（第 1-3 天）：基础搭建
- 创建云开发环境
- 搭建项目结构
- 设计数据库集合
- 开发云函数

### 第二阶段（第 4-7 天）：客户端开发
- 用户登录
- 菜品列表和详情
- 购物车功能
- 订单管理

### 第三阶段（第 8-11 天）：商家端开发
- 商家登录
- 菜品管理
- 订单处理
- 数据统计

### 第四阶段（第 12-14 天）：优化和测试
- 数据实时同步
- 性能优化
- 功能测试
- Bug 修复

### 第五阶段（第 15-17 天）：发布上线
- 小程序提审
- 正式发布
- 用户培训
- 文档整理
