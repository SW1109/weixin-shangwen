# 部署指南

本文档详细说明如何将点餐微信小程序系统部署到生产环境。

## 📋 部署前准备

### 1. 账号准备

- [ ] 微信小程序账号（客户端）
- [ ] 微信小程序账号（商家端，可选单独账号）
- [ ] 腾讯云账号（用于云开发）
- [ ] 已完成微信小程序实名认证

### 2. 开发工具

- 微信开发者工具（最新稳定版）
- Git（用于代码管理）
- Node.js 12+ （用于云函数开发）

### 3. 域名和证书（可选）

- 备案域名（如需使用自定义域名）
- SSL证书（HTTPS）

## 🚀 部署步骤

### 第一步：创建云开发环境

#### 1.1 创建环境

1. 登录[微信小程序后台](https://mp.weixin.qq.com/)
2. 进入"开发" → "云开发"
3. 点击"开通"，选择套餐：
   - **开发环境**：免费额度（适合测试）
   - **生产环境**：按量付费（适合正式上线）
4. 创建成功后，记录环境ID（格式：`env-xxxxx`）

#### 1.2 配置环境

1. 进入云开发控制台
2. 设置环境名称（如：production）
3. 配置基础设置：
   - 数据库：默认配置
   - 云存储：默认配置
   - 云函数：默认配置

### 第二步：配置项目代码

#### 2.1 配置客户小程序

1. **修改 `customer-app/project.config.json`**：
```json
{
  "appid": "你的小程序AppID",
  "projectname": "customer-app",
  "cloudfunctionRoot": "../cloudfunctions/"
}
```

2. **修改 `customer-app/app.js`**：
```javascript
wx.cloud.init({
  env: 'your-env-id', // 替换为你的云环境ID
  traceUser: true,
})
```

#### 2.2 配置商家管理小程序

1. **修改 `merchant-app/project.config.json`**：
```json
{
  "appid": "商家小程序AppID",
  "projectname": "merchant-app",
  "cloudfunctionRoot": "../cloudfunctions/"
}
```

2. **修改 `merchant-app/app.js`**：
```javascript
wx.cloud.init({
  env: 'your-env-id', // 使用相同的云环境ID
  traceUser: true,
})
```

### 第三步：部署云函数

#### 3.1 安装依赖

在微信开发者工具中：

1. 打开"云开发"控制台
2. 选择"云函数"标签
3. 右键点击每个云函数文件夹
4. 选择"上传并部署：云端安装依赖"

需要部署的云函数列表：
- `login` - 用户登录
- `merchantLogin` - 商家登录
- `createOrder` - 创建订单
- `payOrder` - 订单支付
- `updateOrderStatus` - 更新订单状态
- `manageDish` - 管理菜品
- `getStatistics` - 获取统计数据

#### 3.2 配置云函数

每个云函数的 `config.json` 已经配置好，无需修改。

#### 3.3 验证云函数

在云开发控制台 → 云函数，检查：
- ✅ 所有云函数状态为"部署成功"
- ✅ 版本号正确
- ✅ 可以查看日志

### 第四步：初始化数据库

#### 4.1 创建集合

在云开发控制台 → 数据库，创建以下集合：

```
users
categories
dishes
orders
addresses
merchants
carts
```

#### 4.2 设置权限

按照《数据库初始化.md》文档设置每个集合的权限规则。

#### 4.3 导入初始数据

**方法1：手动添加**
按照《数据库初始化.md》文档添加商家账号和菜品分类。

**方法2：批量导入**
1. 准备JSON文件
2. 在云开发控制台 → 数据库 → 导入
3. 选择文件上传

#### 4.4 创建索引

在 `orders` 集合创建索引：
- `orderNo` (唯一索引)
- `createTime` (降序)
- `status` (升序)

在 `dishes` 集合创建索引：
- `sales` (降序)
- `categoryId` (升序)

### 第五步：配置云存储

#### 5.1 创建存储目录

在云开发控制台 → 云存储，创建目录：
```
dishes/      # 菜品图片
categories/  # 分类图标
avatars/     # 用户头像
```

#### 5.2 设置权限

1. 进入云存储设置
2. 设置为"所有用户可读，仅创建者和管理员可写"

#### 5.3 上传测试图片

上传一些示例菜品图片用于测试。

### 第六步：测试小程序

#### 6.1 客户小程序测试

1. 打开微信开发者工具
2. 导入 `customer-app` 目录
3. 点击"预览"，使用手机扫码
4. 测试功能：
   - ✅ 用户登录
   - ✅ 浏览菜品
   - ✅ 加入购物车
   - ✅ 提交订单
   - ✅ 支付订单
   - ✅ 查看订单

#### 6.2 商家管理小程序测试

1. 打开微信开发者工具
2. 导入 `merchant-app` 目录
3. 点击"预览"，使用手机扫码
4. 使用账号登录：admin / 123456
5. 测试功能：
   - ✅ 商家登录
   - ✅ 查看订单
   - ✅ 更新订单状态
   - ✅ 管理菜品
   - ✅ 查看统计

### 第七步：提交审核

#### 7.1 客户小程序审核

1. 在微信开发者工具点击"上传"
2. 填写版本号和描述
3. 登录[微信小程序后台](https://mp.weixin.qq.com/)
4. 进入"版本管理"
5. 选择刚上传的版本，点击"提交审核"
6. 填写审核信息：
   - 功能页面：首页、订单页、个人中心
   - 服务类目：餐饮 → 点餐平台
   - 标签：美食、外卖、订餐
7. 提交等待审核（通常1-7个工作日）

#### 7.2 商家管理小程序审核

1. 重复客户小程序的审核流程
2. 服务类目选择：工具 → 信息查询
3. 注意：商家端可能需要提供相关资质证明

### 第八步：发布上线

#### 8.1 审核通过后发布

1. 登录微信小程序后台
2. 进入"版本管理"
3. 点击"提交发布"
4. 发布后等待生效（通常几分钟）

#### 8.2 发布后验证

1. 搜索小程序名称
2. 测试所有功能
3. 检查数据是否正常

## 🔧 生产环境优化

### 1. 性能优化

#### 1.1 开启CDN加速
在云开发控制台 → 云存储 → 设置，开启CDN加速。

#### 1.2 数据库优化
- 合理设置索引
- 使用分页加载
- 缓存热点数据

#### 1.3 云函数优化
- 优化代码逻辑
- 减少数据库查询
- 使用并发调用

### 2. 安全加固

#### 2.1 数据库安全
- ✅ 已设置权限规则
- 检查敏感数据加密
- 定期备份数据

#### 2.2 云函数安全
- 验证用户身份
- 防止SQL注入
- 限制调用频率

#### 2.3 小程序安全
- 启用HTTPS
- 验证用户输入
- 防止XSS攻击

### 3. 监控告警

#### 3.1 云开发监控
在云开发控制台 → 监控告警：
- 设置云函数调用量告警
- 设置数据库容量告警
- 设置存储空间告警

#### 3.2 小程序监控
在小程序后台 → 统计：
- 查看访问量
- 分析用户行为
- 监控异常情况

## 📊 运营数据

### 1. 查看统计数据

- 在商家管理小程序查看销售统计
- 在微信后台查看用户数据
- 在云开发控制台查看资源使用

### 2. 数据导出

可以通过云函数导出业务数据：
- 订单数据
- 销售报表
- 用户统计

## 🔄 版本更新

### 1. 更新流程

1. 修改代码
2. 更新云函数（如有变动）
3. 上传新版本
4. 提交审核
5. 审核通过后发布

### 2. 灰度发布

在微信后台可设置灰度发布：
- 设置灰度比例（如10%）
- 观察新版本表现
- 逐步扩大比例
- 全量发布

## 💰 费用说明

### 1. 云开发费用

**免费额度**（每月）：
- 数据库：2GB存储，5GB流量
- 云存储：5GB容量，10GB流量
- 云函数：4万次调用，40万GBs资源使用量

**超出免费额度**：
- 按量付费
- 建议设置费用告警

### 2. 小程序费用

- 小程序认证费：300元/年
- 其他功能按需付费

## 🐛 问题排查

### 1. 常见问题

**Q1: 云函数调用失败**
- 检查云函数是否部署成功
- 查看云函数日志
- 检查权限配置

**Q2: 数据库操作失败**
- 检查权限规则
- 查看集合是否存在
- 检查字段类型

**Q3: 图片无法显示**
- 检查云存储权限
- 确认文件路径正确
- 查看网络请求

### 2. 日志查看

**云函数日志**：
云开发控制台 → 云函数 → 日志

**小程序日志**：
微信后台 → 开发 → 运维中心 → 实时日志

## 📞 技术支持

- 微信开放社区：https://developers.weixin.qq.com/community
- 云开发文档：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html
- 问题反馈：在项目中提Issue

## ✅ 部署清单

部署前检查：
- [ ] 已创建云开发环境
- [ ] 已配置项目AppID
- [ ] 已配置云环境ID
- [ ] 已部署所有云函数
- [ ] 已创建数据库集合
- [ ] 已设置数据库权限
- [ ] 已初始化基础数据
- [ ] 已配置云存储
- [ ] 已完成功能测试
- [ ] 已提交审核
- [ ] 已发布上线
- [ ] 已设置监控告警

祝您部署顺利！🎉
